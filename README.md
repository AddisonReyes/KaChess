# PyChess
Chess game written in Python

## Run

- Normal run: `python run.py`
- Hot reload mode: `python run.py --hotreload`

In hot reload mode, the launcher watches Python files and automatically restarts the game when they change.
